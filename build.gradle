apply plugin: 'java'

dependencies {
	compile fileTree( dir: "../libs", include: ['DBCache.jar', 'JUtils.jar', 'SpreadSheetUtils.jar', 'SQLiteModule.jar', 'TerminalWindow.jar'] )
	compile fileTree( dir: "../libs/Apache/POI", include: '*.jar' )
	compile fileTree( dir: "../libs/sqlite-jdbc", include: '*.jar' )
}

ext {
	copyLibs = true
	deployedLibDir = "Apps/run"
	libDest = "deployed/Tools/libs"
	jarPathPrefix = "../../libs/"
	
	mainClass = "application.FileInventory"
}

apply from: '../gradle/utilapp.gradle'